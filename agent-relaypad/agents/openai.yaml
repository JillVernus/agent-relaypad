interface:
  display_name: "Agent Relaypad"
  short_description: "Coordinate cross-agent plan and code reviews"
  default_prompt: "Use $agent-relaypad to check or create a shared cross-agent review."
policy:
  allow_implicit_invocation: true
